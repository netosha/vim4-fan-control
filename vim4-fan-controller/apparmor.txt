#include <tunables/global>

profile vim4_fan flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  # Core capabilities: run Python, bashio, ssh.
  file,
  signal,
  network,

  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /usr/lib/** mr,
  /usr/local/lib/** mr,
  /etc/** r,
  /run/** rwk,
  /tmp/** rwk,

  # Persistent add-on data (ssh key + known_hosts lives here).
  /data/** rwk,
  /share/** rwk,

  # Read-only access to the sysfs nodes we report on.
  /sys/class/fan/** r,
  /sys/class/thermal/** r,
  /sys/devices/**/fan/** r,
  /sys/devices/virtual/thermal/** r,

  # proc for standard Python / bashio introspection.
  /proc/** r,
}
